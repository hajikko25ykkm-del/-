<div class="user-list-container" style="max-width: 600px; margin: 0 auto;">
  <h2>フォロー中</h2>
  <% if @users.exists? %>
    <% @users.each do |user| %>
      <%= render 'user_list', user: user %>
    <% end %>
  <% else %>
    <p style="text-align: center; color: #888;">フォロー中のユーザーはいません。</p>
  <% end %>
</div>